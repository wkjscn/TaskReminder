version: '3.8'

services:
  bh-reminder:
    build: .
    container_name: bh-reminder
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    environment:
      - TZ=Asia/Shanghai
      - NODE_ENV=production
